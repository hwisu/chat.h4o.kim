# Development environment variables template
# Copy this file to .dev.vars and fill in your actual values

# OpenRouter API Key - Get from https://openrouter.ai/keys
# Must start with 'sk-or-v1-' and be at least 64 characters
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Access password for the application
# Use a strong password (minimum 8 characters recommended)
ACCESS_PASSWORD=your_secure_password_here

# JWT Secret for session token signing/verification
# Generate with: openssl rand -base64 32
# Must be at least 32 characters for security
JWT_SECRET=your_jwt_secret_here

# Brave Search API Key - Get from https://api.search.brave.com/
BRAVE_SEARCH_API_KEY=your_brave_search_api_key_here

# Instructions for generating secure values:
# 1. JWT_SECRET: Run 'openssl rand -base64 32' or 'node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"'
# 2. ACCESS_PASSWORD: Use a strong, unique password
# 3. API Keys: Get fresh keys from respective services and revoke old ones 
